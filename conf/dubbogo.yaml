dubbo:
  application:
    name: user-provider
  registries:
    nacosWithCustomGroup:
      protocol: nacos # 注册中心选择 nacos
      address: czytgc.com:8848 # nacos ip
      group: FUNTOUR_DEV # nacos group, 默认 DEFAULT_GROUP
  protocols:
    dubbo:
      name: dubbo
      port: 20000
  provider:
    services:
      UserService:
        interface: com.cai.funtour.api.pub.UserService
  logger:
    zap-config:
      level: error