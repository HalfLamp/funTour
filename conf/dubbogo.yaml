dubbo:
  application:
    name: user-provider
  registries:
    nacosWithCustomGroup:
      protocol: nacos # 注册中心选择 nacos
      address: czytgc.com:8848 # nacos ip
  protocols:
    dubbo:
      name: dubbo
      port: 20000
  provider:
    services:
      UserService:
        interface: com.cai.funtour.api.pub.UserService
        filter: traceFilter
      SightService:
        interface: com.cai.funtour.api.pub.SightService
        filter: traceFilter
  logger:
    zap-config:
      level: error